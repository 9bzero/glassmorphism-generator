# glassmorphism-generator

Generate glassmorphism CSS with live preview.

Adjust blur, transparency, border opacity, and background color. See the effect over a gradient background, copy the CSS.

## Run

```bash
npm install && npm run dev
```
