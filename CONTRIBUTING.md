# Contributing

Pull requests are welcome.

If you're changing how the generated CSS works, include a before/after example in the PR description. Makes review much easier.

```bash
npm install
npm run dev
```
